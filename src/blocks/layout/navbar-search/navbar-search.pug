//-
    name: navbar-search
    description: Markup for the navbar search block.

mixin navbar-search()
    form(method="GET" action="").navbar-search&attributes(attributes)
        +input(
            type="text"
            name="s"
            placeholder="Enter your keyword..."
        )
